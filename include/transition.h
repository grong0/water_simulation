#ifndef TRANSITION_H
#define TRANSITION_H

#include "body.h"
/*
class Transition
{
	public:
		Transition(Body host, Body* targets);

	Body host;
	Body targets[2];
	void update(float deltaTime);
};*/

#endif
